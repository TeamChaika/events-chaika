# Проверки на 19 сентября 2026

## QRM и SMS Aero

- Новый цикл SMS Aero проверен локально: отправка, сохранение ID, сверка доставки, отказ, неопределённый результат, отсутствие слепого повтора, ограничения доступа. Включая существующие проверки оплаты — 36 тестов с локальным PostgreSQL прошли; SQLite: 35 прошли, один PostgreSQL-сценарий пропущен.
- Production-сборка TypeScript/Vite прошла. В браузере подтверждены центрирование пары логотипов и подпись «@ Чайка Тим, 2026».
- Проверка живого QRM-терминала без платежа успешна. Проверена авторизация SMS Aero и бесплатный `/sms/testsend`: провайдер принял формат, реальная СМС не отправлялась. Это не подтверждение настоящего банковского зачисления или доставки на телефон.
- Перед миграцией очереди создана закрытая резервная копия схемы `chaika_events` на существующем сервере Supabase. Секреты находятся только в закрытом локальном файле и настройках хостинга.

## Обновление афиши и заставки

Локально проверены новый пользовательский логотип в шапке и подвале, отсутствие старых разделов и ссылки контролёра, таймер с секундами на первом экране. В браузере проверены красная луна на заставке, более тёмные облака перед ней и переход к афише. После выхода заставки страница доступна мышью и клавиатурой, WebGL продолжает работать.

На реальных ширинах iframe 390 и 351 px нет горизонтального переполнения. Форма открывается, два билета дают 10 000 ₽. Консоль без предупреждений и ошибок. TypeScript и production-сборка прошли. Reduced motion и предельное время ожидания проверены по коду; сбои сети и переключение системной настройки в этой визуальной проверке не имитировались.

После добавления SVG партнёра «Мрия» повторно проверены обе мобильные ширины: логотипы загружаются, группа ChaikaTeam × Мрия помещается в шапке и подвале. Заставка показывает ту же пару логотипов.

## Опубликованное приложение

Timeweb Apps, ID `256931`, основной адрес https://event.chaika.team, технический https://teamchaika-events-chaika-3a5f.twc1.net. Релиз с Supabase `9429b5e9769bd5ad20973c0a4c7e7acad215b797` успешно собран и запущен. Повторный deploy того же кода также завершился успешно; в логах найдены HTTP-порт 8787 и успешная проверка здоровья.

- `/api/health`: `ok=true`; `/api/config`: `demo=false`, `paymentMode=disabled`, `paymentReady=false`. Сертификаты HTTPS проверены без отключения валидации. Для основного домена при распространении DNS дополнительно использован запрос непосредственно на IP Apps с сохранением имени хоста и проверкой сертификата.
- API админки без сессии возвращает 401. Пароль контролёра создаёт роль `door`, которой запрещено создание событий (403). Пароль организатора создаёт роль `admin`. Цена публичного события — 500000 копеек.
- Через production API создано скрытое тестовое мероприятие, загружен PNG, сохранена связь с афишей. После нового deploy событие и изображение остались доступны. После теста удалены только эти две служебные записи. Осталось одно исходное мероприятие, заказов и билетов нет.
- Подтверждено подключение Apps именно к существующему Supabase: база `postgres`, схема `chaika_events`, роль `chaika_events_app`; соединения видны в PostgreSQL. Временный отдельный PostgreSQL остановлен, после этого production API продолжил проходить проверки.
- Семь таблиц защищены ENABLE/FORCE RLS. У `anon` и `authenticated` нет доступа к схеме. Backend-роль не имеет права чтения чужих таблиц в `public`, `auth` и `storage`. Интернет-соединение с базой использует TLS с проверкой CA и имени узла.
- Создана защищённая резервная копия схемы на том же сервере. Внешнее резервирование и восстановление из внешней копии ещё не настроены.

## Браузер опубликованного сайта

Проверено через Codex In-app Browser на техническом адресе Apps:

- Главная афиша, цена 5 000 ₽, WebGL-луна `ready/running`, красное свечение и летучие мыши. Кнопки остановки и возобновления меняют состояние рендера.
- Форма с именем, фамилией, телефоном, email и количеством открывается. Два билета дают 10 000 ₽. Кнопка «Оплата ещё не подключена» отключена.
- На ширинах 390 и 351 px `body.scrollWidth === body.clientWidth`; мобильная афиша и форма проверены визуально.
- `/admin` и `/checkin` открывают соответствующие формы ввода пароля. Ошибок и предупреждений браузерной консоли нет.

## Автоматические проверки

- PostgreSQL 17: 26 из 26 тестов прошли. SQLite: 25 прошли, PostgreSQL-специфичная проверка пропущена.
- Проверены серверный расчёт и сохранённая цена, резерв и лимит мест, повтор запроса, отдельный QR на каждого гостя, однократный проход, чужое мероприятие, роли и Origin/CSRF, приглашения, подтверждение наличных, ограничения количества, запрет production demo, истечение резерва, сверка суммы QRM, недоверие телу callback, неопределённые результаты запросов, SSE и разрешённые платёжные домены.
- Дополнительно проверены сохранность заказа и загруженного PNG при новом экземпляре, однократный проход из двух экземпляров, восстановление только просроченных операций и остановка запуска Apps без PostgreSQL.
- `npm run build` прошёл; `npm audit --omit=dev` — 0 известных уязвимостей. Vite предупреждает о размере отдельно загружаемого Three.js-модуля: 532 КБ, 133 КБ gzip.

QRM в тестах заменён fixture-ответами. Реальные платежи и внешние сообщения не создавались.

## Дополнительная локальная проверка

На localhost в явном демо-режиме пройден сценарий: форма на двух гостей → имитация оплаты → два разных QR → первый проход принят → повторный проход отклонён. В админке проверены редактирование цены, счётчики билетов и проходов. Локальные тестовые продажи не переносились на хостинг.

3D-луна вращается медленно, рендер останавливается при паузе и уходе сцены за пределы экрана, возобновляется при возвращении. Проверены мобильные ширины 390 и 351 px. Настройка reduced motion поддержана кодом и CSS; переключение системной настройки отдельно не проверялось.

## Ещё не подтверждено

- Рабочий QRM-терминал, реальная СБП-оплата, callback провайдера и настройки кассы.
- Получение Telegram, email и СМС настоящими получателями.
- Камера физического телефона и сканирование билета с другого устройства. Серверная проверка той же строки QR проверена; камера реализована через ZXing, работает только онлайн.
- Рабочие реквизиты организатора и условия покупки/возврата. Время 20:00 и вместимость 200 остаются редактируемыми тестовыми значениями.

Оплата и внешняя доставка отключены до подключения рабочих сервисов. Публикация сайта не означает, что реальные продажи уже открыты.
